# SPEC.md 审计报告（/opt/obsidian-Task-Reminder）

- 审计对象：`SPEC.md`（标注版本：**1.3.0**；创建/更新：2026-02-03）
- 审计基线：仓库 `da6c53eb3086d6d4c09965dcf75b2edea9a8a291`
- 审计日期：2026-02-03
- 审计范围：
  - 文档内部一致性（版本、术语、需求、约束之间是否冲突）
  - 与当前仓库实现/元数据的一致性（`manifest.json`/`package.json`/`versions.json`/`src/**`/`README.md`）
  - 可实现性与可验收性（边界条件、错误处理、测试标准是否可落地）
  - 合规与用户预期（数据写入、依赖、同步语义等）

## 结论摘要

规格书结构完整（背景/需求/数据合同/技术设计/错误矩阵/测试与发布计划齐全），且大部分核心功能描述与当前代码实现相符（启动延迟弹窗、每日一次、命令触发、状态栏、Dataview 依赖检测、任务点击跳转、Meeting 高亮等）。

但当前文档存在多处“版本/发布状态/已实现功能”与仓库实际不一致，以及个别章节自相矛盾的问题；其中以 **F4（周期任务生成）与“只读操作”冲突**、**规格书版本与插件版本不一致**最为关键，会直接影响验收与对外发布叙事。

## 严重级别定义

- **P0**：阻塞发布/验收；或造成重大误解（用户预期、合规声明、实现边界）；
- **P1**：重要一致性/可实现性问题；修复成本中等，但不修会导致缺陷或争议；
- **P2**：改进项；不影响基本可用，但提升可维护性/可测性/用户体验；

## 发现清单

### P0-1 规格书版本/发布状态与仓库版本不一致

- 证据：
  - `SPEC.md` 头部标注版本 **1.3.0**，且状态写为“✅ 已发布 - v1.2.0 可用”。
  - `manifest.json` 版本为 **1.1.0**。
  - `package.json` 版本为 **1.1.0**。
  - `versions.json` 仅列出到 **1.1.0**。
- 影响：
  - 规格书无法作为“当前版本”的验收依据，容易出现“文档说已发布但实际未包含”的争议。
  - 对外发布（Release/PR）时版本线无法闭环，影响审查与用户信任。
- 建议：
  1) 明确“规格书版本”与“插件版本”的关系：若 `SPEC.md` 描述的是未来版本，应将状态改为“计划中/草案”，并在修订记录中标注“未落地”范围；
  2) 或者将仓库版本（`manifest.json`/`package.json`/`versions.json`）同步升级并补齐实现与发布物；
  3) 建议在文档增加“对应实现版本/commit”字段，避免漂移。

### P0-2 F4「周期任务生成」与“只读操作”合规声明冲突，且与当前实现不一致

- 证据：
  - `SPEC.md` §2.1/F4 描述：在弹窗中点击按钮“生成到 Daily Note”，并在文件不存在时自动创建、追加写入任务。
  - `SPEC.md` §6.3 “安全合规”写明：**只读操作：不修改用户笔记内容**。
  - 当前仓库实现（`src/**`）未发现写入 Daily Note 的逻辑与 UI（仅展示/跳转）。
- 影响：
  - 若按 F4 实现，将必然写入用户笔记，当前“只读”声明不成立，属于合规/用户预期的重大偏差。
  - 若未实现 F4，但规格书宣称已发布/已整合，会导致验收失败。
- 建议（两选一，需明确）：
  1) **将 F4 明确标注为“规划功能”**（并从“已发布”叙事移出），保留设计但标注未实现；
  2) 若坚持纳入当前版本：更新 §6.3 合规声明与隐私说明（写入范围、触发条件、幂等策略、失败回滚、用户确认/撤销），并在测试计划中加入写入相关用例。

### P1-1 `isDesktopOnly` 的结论前后矛盾

- 证据：
  - `SPEC.md` §6.1 写：`isDesktopOnly` = `false`（支持移动端）。
  - `SPEC.md` §9.3（审计报告回应）最后一行写：`✅ 明确 isDesktopOnly: true`。
  - `manifest.json` 实际为 `isDesktopOnly: false`。
- 影响：
  - 容易导致发布时清单配置错误（移动端不可用或审核不通过）。
- 建议：
  - 以 `manifest.json` 与实际目标为准，修订 §9.3 的结论；并在 §7.4 移动端测试列出最低验收项（Modal/Notice 的可用性差异）。

### P1-2 Nike 数据源：`Done` 字段规则描述与查询条件不一致

- 证据：
  - `SPEC.md` §3.2 Frontmatter 要求写：“`Done: false  # 或不存在`”
  - 同节查询规则写：需要 `"Done" in frontmatter && Done !== true`（意味着 **Done 必须存在**）。
- 影响：
  - 若用户不写 `Done` 字段，按“或不存在”的文档理解，任务本应被纳入；但按查询规则会被排除，产生数据缺失与疑惑。
- 建议：
  - 明确策略（推荐二选一）：
    - **策略 A（宽松）**：`Done` 不存在视为未完成；查询规则与示例应同步；
    - **策略 B（严格）**：要求必须显式声明 `Done`；则删除“或不存在”的表述，并在设置页/文档强调必填字段。

### P1-3 周期任务与 Daily 任务的“去重/归属”规则未闭环

- 现状（文档层面）：
  - §2.1/F1 的“弹窗内容”将周期任务作为来源之一展示；
  - §2.1/F4 又引入“生成到 Daily Note”的机制，并在 §3.4 定义了 `existsInDaily`/`isCompleted` 等状态。
- 风险：
  - 未明确：当周期任务已经存在于 Daily Note 中时，弹窗应显示“Daily 里的那条任务”、还是显示“周期来源任务”、还是两者都显示？
  - 若两者都显示，会产生重复与计数不准确；若只显示一种，需要明确优先级与过滤规则。
- 建议：
  - 在规格书补齐**单一事实来源（SSOT）**与去重规则，例如：
    - 规则 1：周期任务仅用于“待生成”，若 `existsInDaily=true` 则不再作为周期来源展示；
    - 规则 2：Daily 数据源过滤掉以 `🔄` 开头的任务，避免与周期源重复；
    - 规则 3：任务 ID 规范化（同一任务在不同来源的映射规则），保证计数与点击跳转一致。

### P1-4 错误处理矩阵的“用户可见行为”与可验收标准不够落地

- 证据：
  - `SPEC.md` §5 定义了多种场景下的 Notice/设置页提示/禁用来源/重试策略（例如“路径不存在弹 Notice”、“配置格式错误提示行号”、“Dataview 查询超时 >5s”）。
  - 但规格书未给出这些行为的触发条件细则（比如：何时弹一次、是否可关闭、是否在状态栏体现、是否写入日志）。
- 影响：
  - 测试用例难以严格覆盖，且实现者可能随意取舍，导致“矩阵看起来很完整但落地不一致”。
- 建议：
  - 为每个错误场景增加：触发条件、频率（每次/每日一次/仅手动）、用户文案、回退行为、可观测性（console/debug）、以及对应测试用例编号。

### P2-1 “点击跳转并定位到任务行”的描述过于绝对

- 说明：
  - Daily Tasks 天然有行号；但 Nike/Holiday/Recurring 任务往往无法稳定映射到具体行（更多是文件级条目）。
- 建议：
  - 在 §2.1/F1 的描述中补充限定条件：**仅当任务源可提供行号时才定位**；否则仅打开文件并尽量将相关内容置顶/高亮（如有）。

### P2-2 “跨设备同步：不同步”的语义需要澄清

- 风险：
  - 规格书 §2.3 断言“不同步（每设备独立判断）”，但插件数据是否跨设备同步取决于用户是否同步 `.obsidian/`（Obsidian Sync/第三方同步方案可能会同步插件数据）。
- 建议：
  - 用更准确的表述替代绝对结论，例如：
    - “默认存储在插件数据（vault 级）；若用户同步 `.obsidian`，则可能跨设备同步，从而影响‘每日一次’语义。”
  - 如确需“每设备独立”，需在设计中引入设备本地存储方案，并在规范中说明取舍理由与审计点。

### P2-3 测试验收的时间精度指标可能不现实

- 证据：
  - §7.1 “延迟时间 = popupDelay ± 100ms”。
- 风险：
  - JavaScript 定时器在不同平台/负载下抖动较大（尤其启动阶段），±100ms 可能导致不必要的失败。
- 建议：
  - 调整为更宽松且可测的范围（例如：±1s 或“>= popupDelay 且 < popupDelay+N”），或以“用户可感知”作为验收标准。

## 建议的文档修订清单（可直接落地到 SPEC.md）

1. 统一版本与状态：明确当前实现对应版本（建议在首页增加“对应 manifest 版本/commit”）。
2. F4 取舍决策：要么标注为规划并移出“已发布”，要么补齐写入行为与合规说明。
3. 修复 `isDesktopOnly` 矛盾：以 `manifest.json` 为准修正文档与审计回应表。
4. Nike `Done` 字段策略定稿：同步“Frontmatter 要求/查询规则/示例”三处描述。
5. 周期任务与 Daily 去重规则：补齐 SSOT、过滤、计数与点击定位的约束。
6. 错误矩阵落地：为每一行增加触发条件、提示频率、回退与对应测试项编号。

## 附：对齐检查（文档 ↔ 仓库）

- 元数据：
  - `manifest.json`：`id=task-reminder`、`minAppVersion=1.4.0`、`isDesktopOnly=false`（与 §6.1 基本一致）。
  - 当前版本：`manifest.json`/`package.json`/`versions.json` 均为 **1.1.0**（与 `SPEC.md` 1.3.0 不一致）。
- 主要功能：
  - 已实现：F1/F2/F3（见 `src/main.ts`, `src/ui/*`, `src/services/*`）。
  - 未对齐/未体现：F4（周期任务生成/自动创建 Daily Note）、以及 §5 中若干用户提示/超时处理细则。

